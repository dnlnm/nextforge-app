/**
 * Shared error messages that must be safe to import from both server and
 * client components (do not add a `server-only` import here).
 */
export const TENANT_ACCESS_DENIED_MESSAGE =
  "Access denied. You are not a member of this centre. Please contact the centre owner for access.";
