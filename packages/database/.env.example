# --- SHARED VALUES ARE AT THE REPOSITORY ROOT ---
#
# `DATABASE_URL` / `DIRECT_URL` are defined once in the repository root
# `./.env` (`./.env.example` for the template), loaded by
# `prisma.config.ts`. Do not redefine them here.
