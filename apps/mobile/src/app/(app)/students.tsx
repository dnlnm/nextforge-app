import { FeaturePlaceholder } from "@/components/feature-placeholder";

export default function StudentsScreen() {
  return (
    <FeaturePlaceholder
      description="Student lookup and detail will use the existing server-side search endpoint in Phase 5."
      title="Students"
    />
  );
}
