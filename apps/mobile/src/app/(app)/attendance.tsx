import { FeaturePlaceholder } from "@/components/feature-placeholder";

export default function AttendanceScreen() {
  return (
    <FeaturePlaceholder
      description="Attendance marking is the first feature in Phase 5. The secured API mutations are ready."
      title="Attendance"
    />
  );
}
